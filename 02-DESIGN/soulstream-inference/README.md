# 02-DESIGN / soulstream-inference — the mind

The inference plane: the realm serves its agents' thinking as a fleet
of stateless, single-model instances behind capability subjects, with
provider credentials custodied instance-side, a door harnesses reach
credential-free, and the record staying the only record. Code:
[`../../../soulstream-inference`](https://github.com/impire-io/soulstream-inference).

Numbered documents, `0001-` onward:

| Doc | File | What it fixes | Status |
|---|---|---|---|
| 0001 | [`0001-the-inference-plane.md`](0001-the-inference-plane.md) | The founding design: principles (stateless per request, one instance one model, the model an instance attribute, capabilities as realm tools, the record the only record, harnesses credential-free), subjects, the reply grammar with its refusing invariants, instances/adapters, the virtual-name catalogue, the door, the refusals (no orchestration, no router, no conversation store, no realtime), the graduation bars as acceptance criteria | graduated from `inference-plane` (episode 0142) — all five bars measured the day the topic opened; unbuilt, §9 carries the [O]s |
