# Episode 0077 — The canon, held: two channels at equal weight (2026-08-14)

The operator called the drift — "the cassette futurism style is a bit
lost, which makes it look messy" — and the diagnosis was clean: the
vendored tokens always carried the canon's whole vocabulary (bevels,
engraving, textures, mechanical easing); the fast slices had styled
their new surfaces plausibly instead of speaking it. The canon itself
was pulled from the operator's Soulsystem Design System project via
the design-sync lane and vendored into the repo
(`docs/design-canon.md`), and the design pass re-grounded every
surface without moving the approved layout: hairline-or-ink edges,
5 px card radii with the etch, the composer a recessed well, Send a
molded amber key with engraved lettering and press travel, grain on
the page and ribbing on the transport bar, mono label strips for
PEOPLE / STATUS / WAITING ON, work marks as mono badge chips, and the
segmented VU meter as Storage's readout `[measured: on the screens]`.

**The load-bearing fix is semantic.** The canon says amber is the
human channel and teal the machine channel, at deliberately equal
weight — but the screens had been using amber to mean "my messages."
Now: whose a message is rides alignment and nothing else; which
channel a voice speaks on rides the card's edge and the lamp in its
byline. And the record answered the "human or machine?" question in
its own voice: **the protocol removed persona kind outright** (nothing
can verify what controls a key — feature 014's decision, met again
from the consumer position); what it keeps is the countersigned
`operated_by` accountability claim. So the shell reads
**accountability, not species**: a voice that answers for itself is
amber; a voice somebody else answers for is teal, with the operator
named beside it in People. The rig seeds one honestly — Scribe,
operated by Daan, the stamp travelling as a countersigned token — and
the vision sentence renders literally: a human voice and a machine
voice as peers on one surface, at equal weight `[measured: the whole
gate, ~9.5 s uncached at review]`.

Reversal condition: none — records a completed build; the channel
reading (accountability, not species) inherits feature 014's own
rationale and reverses only with it.

Trail: soulstream-shell `505b306`; canon vendored at
`docs/design-canon.md` (re-vendor on canon change — the project wins);
screenshots `shell-v6-canon-conversation.png` /
`shell-v6-canon-home.png` shown to the operator.
