# Episode 0149 — The rc.3 carries the thinking house: v0.14.0-rc.3, workloads v0.8.0-rc.3, the mind's v0.1.0 (2026-08-28)

The two-day arc became installable on the 0132 rails, hours after its
last build. **soulstream-workloads `v0.8.0-rc.3`** — the standing
dispatcher (`specs/011`), `EngineFor`, the declaration's inference
block, and the fleet owned-set race fix; gate green `GOWORK=off`
against the pinned core tag. **soulstream-inference `v0.1.0`** — the
ninth component's **first tag**, cut the day after its repo was
founded: the grammar with its refusing invariants, the one client loop
with `Collect` and `Stream`, the instance kit, both dialects
(anthropic and openai), and the door that speaks both shapes, streams
truly, meters both directions, and never lies about stop reasons.
**soulstream `v0.14.0-rc.3`** — the pins moved to the named tags
(core `v0.14.0-rc.1` and identity `v0.12.0` unchanged), carrying spec
014: the thinking house.

Verified from the outside, the standing standard [measured]: the
release marked prerelease with all four platform tarballs, the tap
formula at `0.14.0-rc.3` on the release's own commit, and the
darwin_arm64 tarball's binary printing its version and answering the
three new hands — `soulstream agent submit`, `model set|ls`,
`provider set`. `brew install impire-io/tap/soulstream` now serves
submit-and-forget: declare an agent naming a virtual model, submit,
walk away.

Not in this rc, queued for the next cut: the shell's declare surface
and its composition (episode 0148, both on mains — the shell wants its
own `v0.11.0-rc.4` first), the same-day wall fixes (workloads
`103a532`: artifact optional where meaningless,
`declaration.DefaultBudget`), and the ceremony constants the shell's
rig awaits on a published soulstream tag.

Reversal condition: none — records a completed release with its
measured verifications; the rc line is judged by the soak, as every
candidate is.

Trail: tags `soulstream-workloads v0.8.0-rc.3`, `soulstream-inference
v0.1.0`, `soulstream v0.14.0-rc.3` (pin bump `0542257`); the tap
`impire-io/homebrew-tap@Formula/soulstream.rb` at `0.14.0-rc.3`; the
arc [episodes 0140](0140-ecosystem-the-focus-agents-as-infrastructure.md)–[0148](0148-shell-the-declare-surface.md);
the previous cut [episode 0139](0139-soulstream-the-rc2-carries-the-tenants-and-the-capabilities.md).
