# Episode 0150 — The rc.4 carries the declare surface: v0.14.0-rc.4, shell v0.11.0-rc.4, workloads v0.8.0-rc.4 (2026-08-28)

The cut episode 0149 queued, made the same evening — the arc's last
human chapter (episode
[0148](0148-shell-the-declare-surface.md)) moved from mains onto named
tags, in the order the pins demanded. **soulstream-workloads
`v0.8.0-rc.4`** — the declare surface's same-day wall fixes
(`103a532`: the artifact optional exactly where it was meaningless,
`declaration.DefaultBudget` as the wake bounds' one source), completed
at the tag with `f2a89d7`: an absent artifact is now absent on the
wire too (`omitempty`, with a marshal test refusing the empty line
nobody wrote); gate green `GOWORK=off`. **soulstream-shell
`v0.11.0-rc.4`** — the declare surface (`bf0258f`) plus `2cac96d`
consuming all three walls: the `file:///dev/null` placeholder gone
from the lane (the JSON-view test refuses its return), the form's
budget prefill reading `DefaultBudget` instead of three restated
numbers, and the rig's two spelled constants become the import they
promised to become — `ceremony.DefaultPlacements` and
`ceremony.AgentRole` off the product's published rc.3, so the
consumer-position gate measures the product's own names (0148's
placed [O], closed). Gate green whole, the consumer-position e2e
29.3s against published tags [measured]. **soulstream `v0.14.0-rc.4`**
— the pins move to both new tags (`994b59b`; core, identity, idp,
inference unchanged), the helm plane's declared facts now composed
from a published shell.

Verified from the outside, the standing standard [measured]: the
release marked prerelease with all four platform tarballs and
checksums, the tap formula at `0.14.0-rc.4` with the darwin_arm64
sha256 matching the downloaded tarball's own hash, and that tarball's
binary printing `0.14.0-rc.4`. `brew install impire-io/tap/soulstream`
now serves the whole storyline of episodes 0140–0148 at every
altitude: declare from a form or a file, submit on your own admission,
the house serves, the mind answers.

Nothing was refuted. Of 0148's ledger, one wall closed at its
promised moment (the ceremony constants); still placed and named: the
fleet submit/node split, the resolve-or-start-by-name discipline
existing twice, catalogue writing from the shell, per-wake keys, the
retirement vocabulary, the live provider arms, and the fresh-eyes
install — which now has its first complete candidate to see.

Reversal condition: none — records a completed release with its
measured verifications; the rc line is judged by the soak, as every
candidate is.

Trail: tags `soulstream-workloads v0.8.0-rc.4` (`f2a89d7`),
`soulstream-shell v0.11.0-rc.4` (`2cac96d`), `soulstream v0.14.0-rc.4`
(pin bump `994b59b`); the tap
`impire-io/homebrew-tap@Formula/soulstream.rb` at `0.14.0-rc.4`; the
build [episode 0148](0148-shell-the-declare-surface.md); the previous
cut [episode 0149](0149-soulstream-the-rc3-carries-the-thinking-house.md).
