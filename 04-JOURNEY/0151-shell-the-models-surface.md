# Episode 0151 — The models surface: names managed, serving read (2026-08-28 → 2026-08-29)

The operator's question the evening the declare surface shipped — should
the shell also manage the thinking side? — answered as design
[`0010`](../02-DESIGN/soulstream-shell/0010-the-models-surface.md) that
same day and built across three repos the next. The Models sheet: every
virtual name as a row saying where it points in the person's words with
the stored truth folded whole, naming and re-pointing as one slide-over
form, removal behind a question that counts the declared agents naming
the name, **Serving now** from the plane's own discovery, the provider
key a folded paste-able command carrying a placeholder and never a
value. The consumer-position e2e walks the whole sheet on a rig running
the serving half of the thinking house — a real stand-in instance
behind the plane — in 6.7s, the whole suite green around it [measured].

The fact-finding before drafting overturned the assessment's central
assumption, in the right direction: the canonical persona scope carries
`$KV.>` (spec 014's persona-wide/agent-narrow asymmetry), so **writing
the catalogue is the person's own act on their own admission** —
mirroring `fleet.Submit`, the surface acts as nobody — rather than a
deployment-lane act. That made the admin gate what design 0005 only
gestured at: a **courtesy line**, drawn and named as one, because the
transport grants every persona the same write and the screen must not
imply otherwise [mechanism-argument, the storage module's honesty rule
applied to a write]; a real chokepoint is a named [O]. Discovery stays
infrastructure's: `$SRV.INFO` is in no persona scope by the plane's own
design, so Serving now rides the shared lane.

The build honored its own upstream ask the same day: the catalogue's
stored shape, bucket and codec became the plane's published contract
(`soulstream-inference/catalogue`, the wire form pinned by its own
test — a change to those bytes is a change to what every realm has
stored), the house's `node/catalogue.go` reduced to its posture
(provisioning, absence-as-ordinary, the record's grammar at the hand),
and every spelled bucket constant retired — the drift episode 0148
flagged, closed at its source. The one-codec claim is measured, not
assumed: the e2e writes a twin through the deployment's own verb and
compares raw KV bytes [measured]. Two honesty walls shaped the form:
`default_params` is stored upstream but applied nowhere on the door
path (`Descriptor.Apply` has no non-test caller), so no editor exists
until the gap closes; and tags made the shell the ecosystem's first
hand for a matching mechanism the CLI never exposed. One new declared
fact, `InferenceOn`, from the config's own word (helm starts before the
inference plane — the `declaredPlacements` ordering), shapes the empty
states only: what serves is discovered, so an instance run beside the
deployment still shows.

One drafting correction of record: the design named the dependency
(`client`, `wire`); the build's allowlist is (`catalogue`, `client`) —
`wire` is never reached. Ledgered in the design in place. What remains
is the publish step, a human act: the inference tag carrying the
catalogue package, the shell and house pins moved onto it, the
temporary e2e replaces dropped — until then co-development rides the
untracked `go.work` and `go mod tidy` waits.

Reversal condition: none — records a completed build against design
0010; the e2e models gate and the grown purity bar are its standing
tripwires. The courtesy-line call reverses if a realm's operator shows
a person outside the admin role re-pointing names in anger — that
reading moves the catalogue-write chokepoint [O] from named to due.

Trail: design
[`0010-the-models-surface.md`](../02-DESIGN/soulstream-shell/0010-the-models-surface.md)
(hq `f231274` the draft, amended with the build); `soulstream-inference`
`513c75a` (the contract); `soulstream` `6635aac` (the rewire, the
fact); `soulstream-shell` `ad86a9f` (the module) + `6a70447` (the e2e
models arm, the thinking rig); the arc [episodes
0140](0140-ecosystem-the-focus-agents-as-infrastructure.md)–[0150](0150-soulstream-the-rc4-carries-the-declare-surface.md).
