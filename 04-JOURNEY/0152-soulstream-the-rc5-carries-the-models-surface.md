# Episode 0152 — The rc.5 carries the models surface: v0.14.0-rc.5, shell v0.11.0-rc.5, inference v0.2.0 (2026-08-29)

The cut episode 0151 queued, made at the operator's word — the models
surface moved from mains onto named tags, in the order the pins
demanded. **soulstream-inference `v0.2.0`** — the catalogue contract
(`513c75a`): the plane's first tag since its founding one, carrying the
stored shape every hand now consumes. **soulstream-shell
`v0.11.0-rc.5`** — the Models sheet (`ad86a9f`/`6a70447`) plus
`ef8037b` moving the inference pin onto the published contract: the
untracked co-dev `go.work` gone, the repo standalone on tags again.
**soulstream `v0.14.0-rc.5`** — the pins move to both new tags
(`fc27c72`; core, identity, idp, workloads unchanged), the helm plane's
`InferenceOn` fact now composed from a published shell.

Verified from the outside, the standing standard [measured]: the
release marked prerelease with all four platform tarballs and
checksums, the tap formula at `0.14.0-rc.5` with the darwin_arm64
sha256 (`f23329…`) matching the downloaded tarball's own hash, and that
tarball's binary printing `0.14.0-rc.5`. `brew install
impire-io/tap/soulstream` now serves the thinking side with its hands:
name a model from the browser, re-point it and every agent naming it
follows, watch what serves from the plane's own discovery.

The last TEMPORARY residue closed behind the tag: the shell e2e's
soulstream replace dropped (`c68afea`) the moment rc.5 published the
rewired `node.CatalogueSet` and the `InferenceOn` fact it was standing
in for — the whole consumer-position suite green against published
tags alone [measured]. Of 0151's ledger, everything named stays named:
the catalogue-write chokepoint, `default_params`' missing door-path
consumer, metering behind inference 0001 §6, the retirement
vocabulary, the live provider arms, the fresh-eyes install — which now
has the models sheet to see too.

Reversal condition: none — records a completed release with its
measured verifications; the rc line is judged by the soak, as every
candidate is.

Trail: tags `soulstream-inference v0.2.0` (`513c75a`),
`soulstream-shell v0.11.0-rc.5` (`ef8037b`), `soulstream v0.14.0-rc.5`
(pin bump `fc27c72`); the replace drop `soulstream-shell c68afea`; the
tap `impire-io/homebrew-tap@Formula/soulstream.rb` at `0.14.0-rc.5`;
the build [episode 0151](0151-shell-the-models-surface.md); the
previous cut
[episode 0150](0150-soulstream-the-rc4-carries-the-declare-surface.md).
